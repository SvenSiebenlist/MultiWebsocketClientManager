[SHOP System]

item0="Name: vServer Factory"
item1="vServer werden alle X Stunden oder 1x am Tag im Shop vom Server generiert.
Man muss also glück haben dass man was gutes im shop findet. Random sind dann werte der Hardware(Leistung) und daraus resultierend die Rarität.

vServer können gemietet oder gekauft werden ? (evtl zu komplex)?

ALLE CLIENTS im eigenen NETZWERK sind daher nur per REMOTE zugänglich und NICHT mittels hardware erweiterbar (nur der lokale router (gateway) kann verbessert werden.

Um fortschritt zu machen ohne den SHOP zu nutzen kann man andere user hacken und ins botnet hinzufügen (was automatisch passiert sobald man den ersten SSH zugriff auf einen user macht...
"

[mechanik]

item0="traffic soll nicht als strich zwischen PCs sichtbar sein, außer man ist mit dem netzwerk verbunden oder es ist ein eigenes paket! (traceroute prinzip)"
item1="oder wenn einem der computer durch ein botnet gehört..."
item2="was mache ich damit neue user nicht von alten usern schickaniert werden?"
item3="BOTNET mit stärke der PCs im netz skalieren. schwache spieler bringen nicht viel außer infos über traffic."

[Software]

item0="default software und selbst geschriebene software (bzw software aus dem \"internet\")
man sollte der quelle vertrauen können da software compiliert wird und dann NUR SCHWER GELESEN WERDEN KANN!"
item1="BOTNET-DETECTOR: detects when this computer is used by botnet users (SSH activity)"
item2="encrypt & decrypt software basierend auf echten verschlüsselungsverfahren
"

[KI basierte spieler die lustig und schlau sind.]

item0="mit chats und aktivitäten"
